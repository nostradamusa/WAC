/**
 * WAC Feature Flags — centralized rollout control.
 *
 * When a feature is ready for public exposure, flip the flag here.
 * All UI guards (scope chips, navbar items, etc.) read from this file.
 */

/** Properties module — real estate listings in the directory */
export const PROPERTIES_ENABLED = true;
